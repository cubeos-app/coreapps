# CubeOS Core Apps

14 Docker services for CubeOS. Each service has:
- `appconfig/` - docker-compose.yml + .env (tracked)
- `appdata/` - runtime data (not tracked)

## Deploy
```bash
/cubeos/coreapps/deploy-coreapps.sh
```
